# Python
__pycache__/
*.py[cod]
.env
.venv
venv/

# SQLite working copy (not needed — update.py now uses CSVs only)
data/jobs.db
data/jobs.sql

# Local clone of source repo used by scrape.py (full historical build)
repo/

# OS
.DS_Store
Thumbs.db